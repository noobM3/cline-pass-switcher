# Cline Pass Switcher - 环境变量配置示例
# 本应用支持完全通过环境变量运行，无需在 Dokploy 中挂载任何存储卷 (Stateless)

# 1. Cline Pass 订阅密钥（必填）
# 支持单个 Key，或逗号/换行分隔的多个 Key 自动组建账号池（例如: sk-key1,sk-key2）
CLINE_PASS_KEY=sk-cline-pass-xxxxxx

# 2. 账号池工作模式（可选: single 手动单账号 | roundrobin 多账号轮询，默认 single）
ACCOUNT_MODE=roundrobin

# 3. 控制台访问密码及下游 API 代理访问凭据（公网部署强烈建议配置，防止盗刷）
PROXY_KEY=your-secure-proxy-key

# 4. 对外公网域名地址（可选，主要用于控制台展示快捷接入指令）
PUBLIC_BASE_URL=https://pass.yourdomain.com

# 5. 目录模型暴露开关（可选: true | false，默认 false 仅返回订阅模型）
EXPOSE_CATALOG=false

# 6. 服务监听端口（默认 3123）
PORT=3123

# 7. 高级配置（可选）：直接传入完整 JSON 配置字符串（包含 perModel 钉住配置等）
# CLINE_PASS_CONFIG={"perModel":{"cline-pass/glm-5.2":{"upstreams":["alibaba"],"pinMode":"strict"}}}

# 8. 额度窗口唤醒（可选，默认开启）：每天定时发一条 max_tokens=256 的真实小请求
#（链路同控制台「发送测试」），让上游从这一刻开始计算额度窗口；失败只记日志与历史，不影响服务。
# 关闭：WARMUP_ENABLED=false
WARMUP_ENABLED=true
# 触发时刻，按 WARMUP_TZ_OFFSET 指定的时区换算（默认 +8 = 北京时间，故 06:00 即北京时间早六点）
WARMUP_TIME=06:00
WARMUP_TZ_OFFSET=8
# 用来唤醒的模型（留空则用默认 cline-pass/deepseek-v4.1-flash）
WARMUP_MODEL=cline-pass/deepseek-v4.1-flash
# 钉住渠道（逗号分隔，按序故障转移）；留空 = 跟随该模型在控制台里的钉住配置
# WARMUP_UPSTREAM=deepseek
# 进程在计划时刻之后才启动时的补发宽限期（分钟），默认 120；0 = 不补发
# WARMUP_CATCHUP_MINUTES=120
